<Window x:Class="Socks5Client.UI.NodeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:Socks5Client.UI.Converters"
        mc:Ignorable="d"
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:EditModeToTitleConverter x:Key="EditModeToTitleConverter"/>
    </Window.Resources>
    <Window.Title>
        <Binding Path="IsEditMode" Converter="{StaticResource EditModeToTitleConverter}"/>
    </Window.Title>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="节点名称:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="服务器地址:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Text="{Binding Server, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="端口:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="代理类型:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="7" SelectedValue="{Binding SelectedProxyType}" SelectedValuePath="Tag" Margin="0,0,0,10">
            <ComboBoxItem Content="SOCKS5" Tag="SOCKS5"/>
            <ComboBoxItem Content="HTTP" Tag="HTTP"/>
        </ComboBox>

        <TextBlock Grid.Row="8" Text="用户名 (可选):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="9" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="10" Text="密码 (可选):" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="11" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="保存" Command="{Binding SaveCommand}" Padding="20,5" Margin="5" IsDefault="True"/>
            <Button Content="取消" Command="{Binding CancelCommand}" Padding="20,5" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

